<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    
  </head>
  <body class="bgColor">
    <%- include ../menu.ejs -%>

    <div class="container-fluid">
      <div class="row section">
        <div class="col-md-4">
          <div class="mr-5 d-inline">
            <b>all Users</b>
          </div>

          <a href="/users/new">Add User</a>
        </div>
        
      </div>
    </div>

    <div class="content">
      <h1><%= title %></h1>

      <% users.forEach(user => { %>
        <div class="card">
          <div class="card-body">
            <h5 class="card-title">Name: <%= user.firstName %> <%= user.lastName %></h5>
            <p class="d-flex mb-3">User Name: <%= user.username %></p>
            <p class="d-flex mb-3">Session Time Out: <%= user.sessionTimeOut %></p>
            <p class="d-flex mb-3">Created Date: <%= user.createdDate %></p>
            <p class="d-flex mb-3">Permissions: <%= user.permissions %></p>
            <button class="btn btn-primary">
              <a href="/users/edit/<%= user.id %>">Edit</a>
            </button>
            <button class="btn btn-primary">
              <a href="/users/delete/<%= user.id %>">Delete</a>
            </button>
          </div>
        </div>
      <% })%>
  </div>

  </body>
</html>
